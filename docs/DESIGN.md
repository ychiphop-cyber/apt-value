# 닥터마빈 아파트 가치진단 — 설계 문서

PRD 68항(구현 전 정리 항목)에 따른 설계 요약. 구현과 함께 유지·갱신한다.

---

## 1. 전체 파일 구조

```
apt-value/
├── build.sh                  # 테스트 → 조립 → 암호화 빌드 (테스트 실패 시 빌드 중단)
├── package.json              # npm test
├── index.html                # 배포본(암호화 게이트 + 암호문). 저장소에는 이것만 실행 산출물로 존재
├── src/
│   ├── head.html             # HTML 골격 + CSS (세금 시뮬레이터 디자인 시스템 승계)
│   ├── engine.js             # 가치평가 엔진 A~E + 하이브리드 결합 + 신뢰도 + 스트레스 (순수함수, Node 테스트 가능)
│   └── ui.js                 # 화면 흐름·렌더링·스트레스 테스트·비교 UI
├── config/
│   ├── valuation-parameters.json   # 모든 계수·가중치·캡·경계값 (코드에 하드코딩 금지)
│   ├── education_hubs.json         # 교육 허브 DB
│   └── job_centers.json            # 업무지역 DB
├── data/
│   └── apartments.json       # 샘플 단지 DB (실거래·전세·입지·수급, 출처·기준일 포함)
├── test/
│   ├── units.js              # 엔진 단위 테스트 (가드·캡·단조성)
│   └── cases.js              # PRD 69 완료기준 사례 회귀 테스트 (전 단지 × 전 평형)
├── tools/
│   ├── build.js              # src + config + data → app.html (config/data JSON을 빌드 시점에 인라인)
│   └── gate.js               # app.html → index.html (AES-256-GCM + PBKDF2 60만회, 비번은 커밋 안 함)
└── docs/DESIGN.md            # 이 문서
```

- 평문 `app.html`, 비밀번호 파일 `.gate-password`는 `.gitignore` — 저장소·배포본에는 암호문만 존재.
- config·data는 **JSON 파일이 원본**이고 빌드 시 앱에 인라인된다. 계수 수정 = JSON 수정 후 재빌드.

## 2. 데이터 스키마 (data/apartments.json)

단위 규약: 가격 **억원**, 월세 **만원/월**, 면적 ㎡, 시간 분, 비율 소수(0.047).

```jsonc
{
  "meta": { "kind": "sample", "asOf": "2026-08", "notice": "데모 샘플 데이터 — 화면에서 수정 가능" },
  "complexes": [{
    "id": "godeok-gracium",
    "name": "고덕그라시움", "city": "서울", "district": "강동구", "dong": "고덕동",
    "regionTier": "서울",            // 요구수익률 지역위험·임대성장률 키
    "builtYear": 2019, "households": 4932, "brandTier": 1,   // 1(메이저컨소)~4
    "parkingRatio": 1.34, "far": 249, "rentalShare": 0.05,
    "redev": { "stage": "none" },     // PRD 32 단계 코드
    "conversionRate": 0.046,          // 지역 시장 전월세전환율
    "areas": [{
      "key": "84", "label": "전용 84㎡ (33~34평형)", "m2": 84.9,
      "trades": [{ "ym": "2026-07", "price": 17.8, "floor": 18 }, ...],   // 매매 실거래
      "jeonse": 8.9,                  // 대표 전세 시세
      "jeonseTrades": [...],          // 선택
      "rentExample": { "deposit": 3.0, "rent": 210 }   // 선택(만원)
    }],
    "location": { "subwayMin": 7, "lines": ["5호선","9호선(연장 예정)"], "transfer": false,
                  "express": false, "futureTransit": "9호선 4단계 (확정)",
                  "jobMinutes": { "GBD": 40, "CBD": 55, "YBD": 65, "JAMSIL": 25, ... } },
    "education": { "elemM": 250, "chopuma": true, "middlePref": 4,        // 1~5
                   "hubId": "godeok-myeongil", "inHub": true, "hubAccess": [{ "hubId": "daechi", "min": 35 }],
                   "age3049": 0.335, "studentTrend": "stable" },
    "life": { "martMin": 8, "deptMin": 25, "hospitalMin": 15, "streetLevel": 3 },   // 1~5
    "nature": { "parkMin": 5, "bigPark": true, "riverMin": 20, "hanRiver": false,
                "hanRiverView": null,   // null = 데이터 없음 → 항목 제외 + 신뢰도 반영
                "forest": true },
    "supply": { "pop": 460000, "next3yAvg": 3900, "adjacentRatio": 1.4, "metroRatio": 1.0,
                "unsoldLevel": 1, "txVolumeLevel": 3, "jeonseListingsLevel": 2,      // 1~5
                "jeonseTrend": "stable", "regulated": true },
  "sources": { "trades": { "src": "국토교통부 실거래가(샘플)", "asOf": "2026-08-12" }, ... }
  }]
}
```

`직접 입력` 모드는 위 스키마의 축약형을 화면 폼으로 채워 동일 파이프라인에 태운다.

## 3. Valuation Engine 구조 (src/engine.js)

모든 함수는 `(input, CFG)`를 받는 순수함수. `input`은 (단지 스냅샷 + 사용자 수정값 + 스트레스 오버라이드) 병합 결과.

```
engineMarket(A)   비교거래 앵커
  - 후보: ①동일단지 동일평형(유사도 1.0) ②동일단지 유사평형(면적탄력 보정×패널티)
          ③인근 유사단지(특성보정×패널티) — 데이터에 comparables로 명시
  - 가중치 w = 유사도 × 0.5^(경과개월/반감기) , 최대 경과월 초과 거래 제외
  - 가중중앙값 = V_market, 가중 p25/p75 = 분산(범위 폭 입력)
engineFinancial(B)   금융·임대 내재가치
  - R(연간 주거서비스) = 월세×12 + 보증금×전환율 | 전세×전환율 (시장 전환율)
  - r = 대체투자수익률 + 유동성 + 지역위험 + 자산위험 (config 합성)
  - r−g < minSpread → 고든 중단, 유한 DCF(40y) 대체   [가드]
  - 역산 g* = r − R/P → "현재가 유지에 필요한 임대가치 성장률"
  - 전세 = 자금조달 구조: 필요자기자본 E = P−J, 전세가율, 전세지지력 5등급
engineHedonic(C)   주거·입지·상품 (0~100 점수 체계)
  - 교통 / 직주(Job Accessibility = Σ 규모×e^(−t/τ)) / 교육(4개 하위: 학교·학원가·접근성·수요지속)
    / 생활 / 자연(한강 접근≠조망 분리, 결측 시 제외) / 상품(연식 비선형·세대수 로그·브랜드 제한·주차)
  - 가격 반영: adj_c = cap_c × (score_c − baseline)/40, 교육은 지역 Education Coefficient 곱
  - Σ를 totalCap으로 클램프, **비교거래 유사도만큼 잔차 축소** (동일단지 동일평형 비교 시 ≈0)
    → 점수는 "왜 이 가격인가" 설명, 가격은 이중가산하지 않음 [PRD 35]
engineSupply(D)   수급·시장구조
  - 간이수요 = 인구×0.5% (간이 수요추정치로 표기), 공급부담률 = 3단계(구/인접/광역) 가중
  - 등급 경계 config, 가격조정 ±cap 제한, 규제 = 수요억제/매물잠김 양면 서술
engineOption(E)   미래 옵션가치
  - 단계→실현확률(config), 용적률 여유 있으면 제한적 프리미엄, 데이터 부족 시 금액 없이 등급+시나리오만
combine()   하이브리드 결합
  - V_mkt_adj = V_market × (1 + 히도닉잔차 + 수급조정)
  - V_fund_eff = V_fund × (1 + 옵션프리미엄)
  - 모델 괴리 d = |차이|/V_market 클수록 fundamental 가중 축소·범위 확대·신뢰도 하락
  - V_center = 정규화 가중합, 시장앵커 ±12% 안전 클램프 [PRD 38]
  - 범위 = V_center × (1±spread), spread = f(거래분산, 괴리, 데이터충족) ∈ [4.5%, 12%]
scores()    3대 점수: 주거가치 / 투자가치 / 가격매력도(현재가 vs 범위 매핑)
confidence()  비교거래 수·최신성·충족률·유사도·수동보정·괴리 → 높음/보통/낮음
applyStress()  오버라이드(금리±, 전세±, 공급×, 가격±) 병합 후 전체 파이프라인 재계산
```

## 4. Config 구조 (config/valuation-parameters.json)

코드에 계수 하드코딩 금지 — 전부 여기서 관리:
`market`(반감기·패널티·특성보정캡) / `financial`(금리·프리미엄·전환율·성장률·minSpread) /
`hedonic`(baseline·카테고리캡·totalCap) / `education`(하위 가중치·지역계수) /
`supply`(수요율·경계값·존가중·캡) / `option`(단계확률·최대프리미엄) /
`final`(결합 가중치·안전클램프) / `range` / `scores`(3대 점수 가중·매핑) /
`confidence`(감점표·등급 경계) / `stress`(프리셋 정의).

## 5. MVP 화면 Flow

```
[잠금] 4자리 비밀번호 (초기 0731, AES 게이트)
STEP 1  아파트 선택 — 검색(단지명/지역) → 샘플 카드 선택 | "직접 입력"
STEP 2  정보 확인 — 평형 선택 → 현재가·전세가·금리·전환율 자동입력값 확인/수정 (출처·기준일 표시)
STEP 3  진단 결과
  HERO(시장가격 / 적정가치범위 / 판단 배지 / 신뢰도) → 3대 점수 타일(클릭=상세)
  → 올리는 요인 / 누르는 요인 → 상대적 가치 기여도 차트 → 조건부 해석(역산 포함)
  → 엔진별 상세(접힘) → 스트레스 테스트(토글 즉시 재계산) → 단지 비교 → 출처·신뢰도·면책
```

## 6. 현재 자동 입력 가능한 데이터 (MVP)

- 샘플 단지 DB 12곳: 실거래 이력·전세 시세·연식·세대수·용적률·주차·입지·교육·수급 — **출처·기준일 표시, 데모 샘플임을 명시**
- config 기본값: 기준금리·주담대·대체수익률·전환율·성장률 (기준일 표시)
- 정적 사이트 제약상 실시간 API 미연결(키 노출 금지, PRD 51). services adapter 구조와 스키마는
  국토부 실거래·부동산원·통계청 연동을 전제로 설계되어 있어 Phase 2에서 어댑터만 교체하면 된다.

## 7. 수동 입력이 필요한 데이터

- 현재 시장가격(최신 실거래/호가 반영 수정), 전세 시세 수정 — STEP 2에서 항상 확인·수정 가능
- "직접 입력" 모드: 샘플에 없는 단지의 핵심 필드(가격·전세·연식·세대수·역거리·학군·수급 수준 등)
- 수동 수정은 신뢰도 계산에 반영(보정 항목 수만큼 감점)되고 결과 화면에 표시된다.

---

## 8. 실거래 자동수집 파이프라인 (v1.1)

네이버부동산·아실은 크롤링이 약관상 금지된 상용 서비스이므로 직접 수집하지 않는다.
대신 그 서비스들의 원천인 **국토교통부 실거래가 공개 API**(매매+전월세)를 직접 수집한다.

```
pipeline/regions.json     수집 대상 시군구 레지스트리 (서울 25 + 수도권 주요, enabled 플래그)
                          + 시군구별 간이 기본값 (tier·인구·전환율·직주시간·공급 — 화면에 '기본값' 표시)
pipeline/collect.js       수집기: 시군구×월 → 파싱 → 단지·평형(floor ㎡) 집계 → 기존 샤드와 병합
data/live/index.json      전 단지 검색 인덱스 (이름·지역·연식·거래량·평형)
data/live/{code}.json     시군구 샤드: 단지·평형별 최근 매매 14건 + 전세 대표값(신규계약 중앙값, 갱신 제외)
.github/workflows/update-data.yml   매일 06:10 KST 최근 2개월 증분 수집·커밋 (시크릿 DATA_GO_KR_KEY)
```

- 해제거래(cdealType=O) 제외, 갱신계약 전세 제외(5% 상한 왜곡 방지), 만원→억 변환, 24개월 이력 유지
- 앱은 잠금 해제 후 index.json을 fetch → 검색 통합. 단지 선택 시 해당 샤드만 fetch
- 자동수집 단지: 가격·전세 = 실데이터, 입지·상품 상세 = regions 기본값 + STEP 2 보완 입력
  → 결측 항목은 `cx.dataGaps`로 엔진에 전달되어 **데이터 충족률·신뢰도에 그대로 반영** [PRD 53·54]
- API 키는 브라우저에 절대 노출되지 않는다 — 수집은 GitHub Actions(시크릿) 또는 로컬에서만 [PRD 51]
- 일 1,000회 호출 한도(개발키) 안에서: 일상 갱신 = 지역×2개월×2API ≈ 160회, 백필은 나눠 실행

---

## 9. Station Intelligence Engine (v2.0)

"역세권인가?"가 아니라 **"얼마나 강력한 역을 얼마나 가까이 이용할 수 있는가"**를 정량화한다.

```
data/rail_network.json          수도권 철도망 그래프(19개 노선 구간·381역·급행 패턴·좌표는 근사)
                                + 역별 목적지 속성(고용·상업·문화·의료·관광 1~5, ESTIMATED 초기값)
pipeline/station_intel.js       (역,노선) 확장 정점 다익스트라(환승 4분 페널티) →
                                A 업무지 접근성(Σ중요도×e^-t/τ) · B 목적지가치 · C 네트워크(직결 커버리지·허브근접·노선수·급행)
                                · D 경제활동 → Station Value(가중 config) + 강남 접근성 별도 산출
data/station_intelligence.json  역별 SV·컴포넌트·순위·백분위·업무지별 분·areaPriceLevel(콘텐츠) 캐시
data/line_intelligence.json     황금노선 지수 = 60% 전체평균 + 25% 상위20% + 15% 네트워크 효용
data/dong_stations.json         법정동→역 자동 연결(ESTIMATED) — 화면에서 확인 시 MANUAL 전환
```

- **기존 교통·직주 점수를 교체**(가산 아님): hedonic.transport = f(주력역 svT×거리감쇠×(1+추가네트워크 보너스)),
  hedonic.job = 역 기반 업무지 접근성. 카테고리 캡·총량 캡 그대로 → 가격 영향 상한 유지 [§45·46]
- 더블역세권: 노선 개수 보너스 금지 — 두 번째 역이 **새로 여는 업무지 가치**에 비례(≤25%/≤10%, 총 30% 캡) [§22·23]
- Station Wealth·areaPriceLevel(교통 대비 가격)은 **콘텐츠 전용** — 가치평가에 미사용, 순환참조 차단 [§11·36]
- 데이터 상태 체계: VERIFIED/ESTIMATED/MANUAL/UNKNOWN — 미확인은 중립 처리+신뢰도 감점, 몰래 대체 금지 [§39·40]
- 역별 점수 하드코딩 없음 — 전 값이 그래프·config에서 계산되고 test/station.js(§48 Test A~E)가 회귀 가드
- UI: 결과 내 "교통 가치 분석"(내 역의 힘·한 정거장 비교) + 진단형 문장 4종 + 역 가치 지도
  (SVG 버블맵, 역가치/경제력 모드, 호선 보기·황금노선 카드, TOP 랭킹, 역 카드·교통 대비 가격)

---

## 10. V3 전면개편 (2026-08-19) — Data Integrity + Valuation V3 + Station V3

**P0-1 실거래 정합성**
- 원인 규명: 상세 프로필 단지의 수기 샘플 거래가 실데이터를 가림 → `mergeSampleWithLive()`가
  프로필은 유지하고 실거래·전세만 국토부 자동수집 데이터로 교체 (고덕그라시움 18.3억(가짜) → 25.45억(실제))
- 수집기: **Monthly Full Replace** (재수집 월의 기존 거래 전체 삭제 후 원본 교체 — 취소·정정 반영),
  계약일 보존(YYYY-MM-DD), aptSeq 보존(전월세 응답), 이상거래 플래그(12개월 중앙값 ±28%, 삭제 아닌 저가중),
  네트워크 재시도(3회)·일시 오류 시 해당 월 건너뜀(기존 데이터 보존)
- **시장 기준가 ≠ 최근 실거래 1건**: 90→180→365일 기간창 가중중앙값(최신성 반감 75일·이상거래 ×0.2·저층 ×0.85),
  기간 확장 시 화면 표시 + 신뢰도 감점
- 2026 행정개편 반영: 부천 3구 재설치(41192·41194·41196)·화성 권역 분구(41591~41597), 폐지 코드 제거

**P0-2 단지 기본정보**: `pipeline/complex_info.js` — K-apt 공동주택 API 어댑터(단지목록+기본·상세, 후보 endpoint 순차 시도).
활용신청("공동주택 단지 목록제공"+"기본 정보제공") 전까지 세대수·주차·용적률은 UNKNOWN 유지(임의 기본값 폐기).

**P0-3 가치평가 V3**: 하나의 '적정가' 폐기 → ① 시장 기준가(범위) ② 금융 지지가치(g 보수/기준/우호 시나리오 범위)
③ 장기 경쟁력 Score(밴드) + 판정 3종(시장 상대평가 할인/적정/프리미엄 · 금융 지지력 4등급 · 미래 기대 반영도 4등급).
'고평가' 단정 금지 — 금융수익률 낮음 = 프리미엄의 존재이며, 무엇이 그 프리미엄을 정당화하는지 설명(§20~22).
UNKNOWN 항목은 계산에서 제외하고 가중치 재정규화(§12). Calculation Trace + ?debug=true.

**P1 미래가치 V3**: 5축(수요지속 25 / 공급희소 25 / 교통·일자리 변화 20 / 교육선호 15 / 정비옵션 15, config)
→ 장기 성장률 g 시나리오 산출(지역 prior ± 스윙) → 역산 g*와 비교해 "미래 기대 반영도" 판정(§28~29).

**P0-4 Station V3**: Generalized Travel Time(대기 배차/2×0.6 + 환승도보 3분 + 심도 진입 1.5/3/5분) +
Service Friction(배차·요금·심도) + **Golden Corridor Index**(노선이 관통하는 고가치 벨트 최고 연속구간, sv 0.7+wealth 0.3)
+ 점수 백분위 정규화(상위 1%≈95+, 평균권 45~65). 결과: 2호선 98 > 3호선·9호선·신분당 87 ≫ 공항철도 60 · GTX-A 51
(§37~43·81 충족 — 조작 없이 구조로 달성).

**P1 결과 UX**: HERO = 현재가(+최근 실거래 별도 표시) / 시장 기준가 / 금융 지지가치 / 장기 경쟁력 + 판정 배지 3종
+ "이 아파트를 한 문장으로 보면" + 미래가치 아코디언(5축·g·역산 비교) + 데이터 상태(VERIFIED/ESTIMATED/MANUAL/UNKNOWN).

**P1 지도**: Leaflet 실지도(줌·팬·fit-to-line·줌레벨별 라벨) + Golden Corridor 구간 강조 + 노선별 역 가치 프로필
(가로 스크롤 바) + 역가치/역세권경제력 모드. Leaflet 로드 실패 시 SVG 개념도 폴백. 동명 충돌 가드(시군구:동 오버라이드).

## 11. V3.1 — Station Value V4 (역 가치 재정의, 2026-08-19)

역 가치가 업무 접근성에 치우쳐 주거 강지역(대치·목동·고덕·올림픽공원 등)이 저평가되던 문제를 수정.

- **Station Value 4축** (config `station.v4.axes`): 교통·네트워크 30%(강남·도심·여의도 체감시간 + 네트워크 + 운행 마찰) / 역세권 경제력 35%(역 생활권 법정동 실거래 ㎡가 중앙값 winsorize 백분위, 표본<3이면 수동 wealth 폴백 — 소득 데이터 추정 생성 금지) / 교육·주거 생활권 20%(학원가 허브 거리감쇠 + 단지 밀집도) / 업무·도시 중심성 15%(주변 업무·상업 시설). 각 축은 전 역 백분위 정규화 후 가중합 — 단일 변수 지배 방지.
- **순환참조 방지**: 아파트 평가용 svT는 `axesForValuation`(교통 55/업무 25/교육 10/경제력 10)으로 경제력·교육 축 축소 — 단지 자체 시장기준가·교육점수와 중복 계상 차단. 동 단위 중앙값이라 개별 단지가 역 점수를 좌우하기 어려움.
- **노선 가치 V4** (`v4.lineV4`): 중앙값 50% + 상위 25% 역 평균 30% + 핵심지 연결성 20% — 초고가 역 1~2개의 견인과 외곽 구간에 의한 희석을 동시에 방지. breakdown(중앙값·상위평균·핵심연결성)을 노선 카드에 표시.
- **동명 충돌 스코프 키**: dong_stations에 11680:신사동(강남)/11380:신사동(은평, 링크 없음)·11470:신정동(양천)/11440:신정동(마포)·41290:중앙동(과천)/41133:중앙동 추가 — 은평 신사동 아파트가 강남 신사역에 연결되던 버그 수정. station_intel도 동일 키 체계로 지역 스코프 가격 풀링.
- **지도**: 원 크기 = 비선형 곡선(5~22px, 50점 vs 90점 즉시 구분), 원 색 = 노선 대표색(rail_network 색 재사용), 겹테두리 = 환승역, 범례에 실제 크기 샘플. 역 카드에 4축 백분위 + 세부(핵심접근·마찰·학원가·밀집도). TOP 10 + 전체 역 보기(4축 병기).
- **§12 모델 설명**: 결과 화면에 "이 아파트의 가치는 어떻게 계산했나요?" 카드 — 실제 config 가중치, 항목별 점수·재정규화 가중치·기여도·평가 이유 표, 데이터 소스, 한계·신뢰도. 표시 숫자는 전부 런타임 config/결과값.

## 12. V3.2 — Station·Line Value 분리 + 경제력 단지 단위 재구성 (2026-08-19)

Station Value(역 생활권 가치)와 Line Value(노선 네트워크 가치)를 서로 다른 모델로 분리. "왜 이 역/노선/아파트인가"를 화면에서 설명 가능하게.

- **역세권 경제력 V5** (`station.v4.econ*`): 거래 → 평형(최근 거래 중앙값, o플래그+밴드 이상치 제거) → 단지 대표 ㎡가(평형 값 중앙값, **단지당 1표**) → 역세권 **도보시간 가중**(econWalkCurve: 5분 1.0 → 15분 0.35) × **규모 가중**(min(매매+전세 관측수, 40) — 세대수 부재 시 규모 proxy; 전세 포함으로 거래허가구역 대단지의 규모 소실 방지) **가중 중앙값** → winsorize [2,98] → 동률 평균 순위 백분위. 관측 2건 미만 단지 제외. 효과: 잠실주공5 재건축 ㎡가가 잠실새내를 지배하던 문제, 목동 나홀로 소형 단지가 신시가지를 희석하던 문제(econ 47→62) 해소. 법정동 일괄 귀속 완화(같은 동이라도 역별 도보 가중 상이). 단지당 1표 + 중앙값 = 평가 대상 단지의 자기강화 차단.
- **Line Value V5** (`station.lineV5`, 5축 — Station Value 평균 아님): ① 핵심 생활권 연결력 30% — `config/life_hubs.json` 41개 생활권(유형·티어·소속 역) 중 서로 다른 생활권 직결 수(같은 생활권 반복 가산 금지, 1역 스침은 0.65 감쇄, **§15 환승 편승 감쇄**: 취급 역들의 환승 노선 수 평균 감쇄) ② 노선 내 역세권 가치 25% — 환승 감쇄(1노선 1.0/2노선 0.85/3+ 0.72) SV의 중앙값 45+상위25% 35+80점↑ 비율 20 ③ 도시 횡단·네트워크 20% — 연결 관계 4종(주거↔업무/주거↔교육/업무권간/광역 관문, '취급' 생활권만 인정) + 타 노선 환승 + KTX·공항 관문 (노선 길이 자체 무점수) ④ 실제 이동 효율 15% — 배차 + 표정속도(역간거리/시간) + 급행 ⑤ 독점성 10% — SV 가중 단독 취급 비중 + 생활권 독점 연결(≤2노선). 결과: 5호선 98 / 9호선 97 / 3호선 96 / 수인분당 94 / 2호선 93 / 신분당 74 … 공항철도 45(§15 편승 제거: 독점성 22) — 순위 하드코딩·보정 없음.
- **§33 정합성**: station·line JSON은 같은 meta(version 5·generatedAt)로 함께 생성, 불일치 시 테스트 실패 + 앱 부팅 경고. build.sh가 파이프라인 → 테스트 → 빌드 순서 강제.
- **TOP10 카테고리 탭**: 종합/교통/경제력/교육·주거/업무 — 선택 기준으로 TOP10·전체보기 정렬, **지도 원 크기도 동일 기준**으로 변경(원 색 = 노선색 유지). 비종합 탭은 종합 순위 병기, 경제력 탭은 표본 부족(manual) '추정' 배지. 역 카드에 "현재 선택 카테고리 ○위". Leaflet attribution prefix 숨김(OSM 출처 유지).
- **대표 최근가** (`marketRef.anomalyLow`, engine `repRecentPrice`): 직전 거래가 최근 3개월 또래 중앙값×0.85 미만(또는 o플래그)이고 또래 ≥2건이면 **최근 3개월 최고가로 표기·자동입력** — 헬리오시티 59㎡ 19.3억(특수거래 의심) → 26.95억. 최근 거래 우선 원칙 유지, 원거래는 사실대로 표시 + 보정 사유 문구.
- 노선 카드: 5축 바 + 주요 연결 생활권 칩(★최상위 ◆독점) + "노선 가치는 역 평균이 아닙니다" + 환승 감쇄 명시. 강남역 생활권 링크 보완(서초동·역삼동 → 강남역).

### 12.1 V3.2.1 — Line Value 중복 보상 제거 (2026-08-19)

V5에서 5호선이 1위까지 오른 원인 = 같은 장점(다양한 생활권·횡단·환승)이 생활권/네트워크/독점성 3축에서 중복 보상 + 표시 재스케일(22~98)이 작은 raw 차이를 과장. 수정(5호선 감점·특정 노선 가산 없음):
- 가중치: 역세권 30(← 25) / 생활권 25(← 30) / 네트워크 20 / 효율 15 / 대체불가 10.
- 생활권 축: 한계효용 체감 — 기여 내림차순 i번째 ×0.82^i (`hubDiminish`). 생활권 개수 누적으로 긴 노선이 유리해지지 않음.
- 네트워크 축: 주거↔업무·주거↔교육 관계 제거(생활권 축과 중복) → 환승(45)·광역 관문(25)·동서남북 횡단성(30, 시청 반경 16km 내 스팬만·18km 포화)만.
- 대체불가 축: soloShare(단독역 수) 삭제 → **detour** = 해당 노선을 그래프에서 제거하고 강남·도심·여의도 체감 도달시간 재계산, 소속 역 증가분의 SV 가중 평균(20분 포화) + 생활권 독점 연결 40%.
- 재스케일 제거: 최종 점수 = 5축 가중합 raw 그대로(소수 1자리). Tier S/A/B/C(최고점 −4/−12/−24) 표시로 근접 점수 과잉 서열화 방지.
- 테스트: 순위 강제 테스트 전부 삭제(공항철도>5호선 등) → 구조 테스트로 교체(감쇄·체감·무가산·soloShare 제거·동일 데이터 재현 일치).
- 결과: 9호선 77.5 / 3호선 76.4 / 2호선 75.3 / 5호선 73.7 (S) / 수인분당 71.4 (A) / 4·7·1호선·신분당 63.7~64.4 (B) / 공항철도 49.6 (C, 우회 실측 11 — 편승 차단 실증).

### 12.2 V3.2.2 — 경제력 = 거주민 경제수준 (집값·업무·상권 분리, 2026-08-19)

경제력 축을 "그 역 생활권에 거주하는 주민들의 경제 수준(소득·소비)"으로 재정의. 이전 버전은 아파트 실거래 ㎡가를 경제력 입력으로 써서 ①집값→역 가치→아파트 가치 순환참조 ②고속터미널처럼 상권·고가 시세가 '거주민 경제력'으로 둔갑하는 왜곡이 있었다.
- **입력 = w 등급(1~5)만**: rail_network의 역세권 거주민 경제수준 전문가 추정 등급(ESTIMATED, 380/381 커버) → 전 역 백분위(동률 평균 순위 → {98/87/60/21/…} 5단계). 시세·업무지·상권·유동인구 완전 제외 — 업무는 d.emp 등(업무 축), 상권은 d.comm(업무 축)에서만 평가.
- **시세는 priceLevel(검증용)로 분리 보존**: 기존 단지 단위 시세 계산(단지 1표·도보/규모 가중)은 유지하되 점수 미반영. 역 카드에 "참고: 주변 시세 백분위 N (평가 미반영·검증용)" 표시 + 파이프라인이 등급 vs 시세 괴리 상위(등급 재검토 후보)를 리포트.
- **한계 인정**: 역세권 단위 소득·자산 정밀 데이터 미확보 — 5단계 추정 등급임을 UI에 명시("5단계 추정" 배지 + "근거: 거주민 소득·소비 수준 ○○권"), 임의 정밀값 생성 금지. 등급이 같으면 동률(경제력 탭에 등급 병기).
- 테스트: 경제력 점수 distinct ≤ 5 가드(연속 시세 변수 유입 시 실패), 같은 등급이면 상권 大(고속터미널)=상권 小(잠실새내)·업무 大(삼성)=업무 小(목동) 동점 구조 증명, 압구정 최상위권 sanity, priceLevel 독립 필드.
- 결과: 경제력 최상위 = w5 16개 역(압구정·대치·반포·잠원·도곡·개포·판교 등 98점) / 강남·삼성·잠실·목동 87 / 서울역·종로3가 21. SV TOP: 신사 98·고속터미널 98·신논현 98·교대 98·선릉 98·강남 97. 노선: 9호선 77.6 / 3호선 76.7 / 2호선 74.9 (S) / 5호선 72.9·수인분당 72.1 (A) / 신분당 65.2 (B).
